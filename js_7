function weightCheck (weight) {
let recommendation = ['Time to eat', 'The weight is normal', 'Time to workout'];
 if (weight < 4) {
	 return recommendation[0]; 
 } else if (weight >= 4 && weight <= 5.5) {
	 return recommendation[1]; 
	 } else {
		 return recommendation[2];
	 };
};
weightCheck (1);
